% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_structural_shocks.R
\name{compute_structural_shocks.PosteriorBSVAREXH}
\alias{compute_structural_shocks.PosteriorBSVAREXH}
\title{Computes posterior draws of structural shocks}
\usage{
\method{compute_structural_shocks}{PosteriorBSVAREXH}(posterior)
}
\arguments{
\item{posterior}{posterior estimation outcome - an object of class 
\code{PosteriorBSVAREXH} obtained by running the \code{estimate} function.}
}
\value{
An object of class PosteriorShocks, that is, an \code{NxTxS} array with attribute PosteriorShocks 
containing \code{S} draws of the structural shocks.
}
\description{
Each of the draws from the posterior estimation of models from
packages \pkg{bsvars} is transformed into
a draw from the posterior distribution of the structural shocks.
}
\examples{
# specify the model
specification  = specify_bsvar_exh$new(us_fiscal_lsuw)

# run the burn-in
burn_in        = estimate(specification, 10)

# estimate the model
posterior      = estimate(burn_in, 10)

# compute structural shocks
shocks         = compute_structural_shocks(posterior)

# workflow with the pipe |>
############################################################
us_fiscal_lsuw |>
  specify_bsvar_exh$new() |>
  estimate(S = 10) |> 
  estimate(S = 10) |> 
  compute_structural_shocks() -> ss
  
}
\seealso{
\code{\link{estimate}}, \code{\link{normalise_posterior}}, \code{\link{summary}}
}
\author{
Tomasz Woźniak \email{wozniak.tom@pm.me}
}
